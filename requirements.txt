# Core LangChain and document processing
langchain==0.1.14
langchain-community==0.0.38
pypdf==4.0.1

# Vector store (FAISS for local storage)
# Updated to 1.13.2 for Python 3.13 compatibility
faiss-cpu>=1.13.0,<2.0.0

# Hybrid retrieval (BM25 + Vector Search)
rank-bm25==0.2.2

# Embeddings (open-source) - OPTIONAL: install locally for better accuracy
# sentence-transformers==2.2.2  # Optional: uncomment to use HuggingFace embeddings (requires torch)
# If not installed, the system will fall back to TF-IDF embeddings via scikit-learn

# TF-IDF fallback embeddings (required)
scikit-learn>=1.2.2

# LLM - Ollama for local/open-source models (mistral, llama, etc.)
# Note: Ollama must be installed separately from https://ollama.ai
# Run: ollama run mistral

# Web interface
streamlit==1.28.1

# Utilities - Updated for Python 3.13 compatibility
python-dotenv==1.0.0
pydantic>=2.5.0
requests>=2.31.0
tqdm>=4.66.1
numpy>=1.26.0
pandas>=2.1.3

